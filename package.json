{
  "name": "bootstrap-color-generator",
  "version": "1.0.0",
  "description": "A CLI tool to generate Bootstrap colors with accessibility checks.",
  "main": "generate-colors.js",
  "scripts": {
    "start": "node generate-colors.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bootstrap",
    "color",
    "scss",
    "accessibility",
    "chroma.js",
    "cli"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "chroma-js": "^2.4.2"
  },
  "bin": {
    "generate-colors": "./generate-colors.js"
  }
}
